import type { Components } from "@mui/material/styles";
import { MuiButton } from "./button";

import type { Theme } from "../types";

export const components = { MuiButton } satisfies Components<Theme>;
